# Loop Memory

This directory stores concise operational memory for loop-engineered Codex work.

Do not store private chain-of-thought. Store only durable facts useful for future runs:

- loop id
- run id
- goal
- files touched
- checks run
- terminal state
- blockers
- next action

Active loop files go under `active/`.
Completed or summarized loop files go under `archive/`.
